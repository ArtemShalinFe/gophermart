version: "3.9"
services:
  
  postgres:
    image: postgres:15.3
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: "/var/lib/postgresql/data"
    volumes:
      - ./db/init:/docker-entrypoint-initdb.d
      - ./db/data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  accural:
    build:
      context: ./..
      dockerfile: deployments/accrual/Dockerfile
    depends_on:
      - postgres
    environment:
      - ACCRUAL_ADDRESS=localhost:8080
      - DSN='postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/accrual?sslmode=disable'
    restart: always

  gophermart:
      build:
        context: ./..
        dockerfile: deployments/gophermart/Dockerfile
      depends_on:
        - postgres
      environment:
        - RUN_ADDRESS=${RUN_ADDRESS}
        - DATABASE_URI='postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/gophermart?sslmode=disable'
        - ACCRUAL_SYSTEM_ADDRESS=accural:8080
      restart: always